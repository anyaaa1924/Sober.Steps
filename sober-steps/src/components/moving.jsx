// src/components/MovingLine.js

import React from 'react';
import './home.css'; // Ensure this file contains the .moving-line styles

const MovingLine = () => (
  <div className="moving-line">
    <p>Sobriety is not a destination, but a journey.</p>
  </div>
);

export default MovingLine;
